{
  "name": "http-server",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "dev": "bun --watch run ./index.ts",
    "server": "bun run ./index.ts",
    "start": "bun run ./index.ts"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.6"
  }
}
